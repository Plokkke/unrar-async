export * from './js/types';

// Browser support: TODO — implement Web Worker-based extraction
// For now, the browser build re-exports types only.
// Use the Node.js build (index.ts) for full extraction support.
